import React from "react";
import ReactDOM from "react-dom/client";
import CenturyCompare from "./CenturyCompare.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CenturyCompare />
  </React.StrictMode>
);
